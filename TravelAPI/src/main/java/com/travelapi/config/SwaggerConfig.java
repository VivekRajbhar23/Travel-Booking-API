package com.travelapi.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Using springdoc-openapi starter auto configuration.
    // Swagger UI will be available at /swagger-ui.html or /swagger-ui/index.html
}
